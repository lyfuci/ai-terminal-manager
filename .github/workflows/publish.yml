name: publish

# 打 v* tag 就发 PyPI。用 trusted publishing，仓库里不存任何 token：
# 在 pypi.org 项目设置里加 GitHub publisher（owner lyfuci / repo ai-terminal-manager /
# workflow publish.yml / environment pypi），再在 GitHub 仓库 Settings → Environments 建一个叫 pypi 的环境。
on:
  push:
    tags: ["v*"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: sudo apt-get update && sudo apt-get install -y tmux
      # tag 直接发 PyPI，之前没有任何门：这里至少把测试和 lint 跑一遍再 build。
      - run: uv sync --frozen
      - run: uv run --frozen ruff check .
      - run: uv run --frozen pytest
      - run: uv build
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
