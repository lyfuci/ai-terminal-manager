"""消息翻译表。键 = 源码里的中文原文（见 i18n.py）。

约束（tests/test_i18n.py 会查）：
- 每个 `_()` 里的字符串都必须在 EN 和 JA 里有条目 —— 漏了测试直接红。
- 占位符 `{name}` 必须和原文一模一样（含 `!r` 和 `:%Y-%m-%d` 这类格式），少一个就 KeyError。
- 表里不能有源码里已经不存在的键（防止翻译表越滚越大）。
"""

from __future__ import annotations

EN: dict[str, str] = {
    "== 数据源 ==": "== Data sources ==",
    "\n== 持久化（resurrect + continuum）==": "\n== Persistence (resurrect + continuum) ==",
    "\n== 内存闸门 ==": "\n== Memory gate ==",
    "\n== 索引 ==": "\n== Index ==",
    "手动存/恢复：prefix + Ctrl-s / prefix + Ctrl-r；": "Manual save / restore: prefix + Ctrl-s / prefix + Ctrl-r;",
    "自动存档每 10 分钟，只在有客户端 attach 时触发": "autosave every 10 minutes, only while a client is attached",
    "  直接敲 claude / codex 不受以上任何限制；要限就用 atm claude / atm codex": "  A plain `claude` / `codex` is not limited by any of this; use `atm claude` / `atm codex` to opt in",
    "AI Terminal Manager — Claude Code / Codex / Pi 的统一会话历史，投到指定 tmux pane": "AI Terminal Manager — unified Claude Code / Codex / Pi session history, dispatched into any tmux pane",
    "多打点过程信息到 stderr（-v 概要，-vv 逐文件/逐条 tmux 命令；ATM_DEBUG=1 同 -vv）": "More progress info on stderr (-v summary, -vv per file / per tmux command; ATM_DEBUG=1 equals -vv)",
    "列出会话": "list sessions",
    "最多列几条（默认 30）": "max entries to show (default 30)",
    "输出 JSON": "output JSON",
    "交互选一条会话并投递（主命令）": "pick a session interactively and dispatch it (the main command)",
    "预填搜索词": "pre-filled search query",
    "列表怎么聚合：cwd(目录，默认) / agent / none(平铺)。TUI 里 Ctrl-G 可循环切换": "grouping: cwd (directory, default) / agent / none (flat). Ctrl-G cycles inside the TUI",
    "直接投到这个 pane（跳过选目标那一步）": "dispatch straight into this pane (skip the target picker)",
    "投到新分出来的 pane": "dispatch into a newly split pane",
    "投到新 window": "dispatch into a new window",
    "--split 时上下分而不是左右分": "with --split: split vertically instead of horizontally",
    "只打印命令，不投递": "print the command only, do not dispatch",
    "目标 pane 正忙也照投": "dispatch even if the target pane is busy",
    "不给会话套 cgroup 内存闸门（默认套）": "do not wrap the session in a cgroup memory gate (on by default)",
    "软上限，超了节流+回收不杀（默认取 atm config）": "soft cap: throttle + reclaim, never kills (default from atm config)",
    "硬上限，回收压不住才杀（默认取 atm config）": "hard cap: kills only when reclaim can't keep up (default from atm config)",
    "按 id 直接投递（不进 TUI）": "dispatch by id directly (no TUI)",
    "会话 id（可以只写前缀）": "session id (a prefix is enough)",
    "投到这个 pane": "dispatch into this pane",
    "从当前 pane 分一格出来投": "split a pane off the current one and dispatch there",
    "新开 window 投": "dispatch into a new window",
    "只打印命令，不投": "print the command only",
    "配合 --split：上下分而不是左右分": "with --split: split vertically instead of horizontally",
    "构建/查看索引": "build / inspect the index",
    "清缓存后全量重建": "clear the cache and rebuild from scratch",
    "列出 tmux pane": "list tmux panes",
    "常驻侧栏：列出运行中的格子和历史，选中换进主格": "persistent sidebar: running panes and history; select to swap into the main pane",
    "开/切/收侧栏：没开就在当前 window 最左边开一个；开了就切过去；焦点在侧栏上就收起": "open / switch to / collapse the sidebar: open on the far left if closed; switch if open; collapse if focused",
    "--toggle 时「当前 pane」是哪个（键位绑定里传 #{pane_id}）": 'which pane is "current" for --toggle (the key binding passes #{pane_id})',
    "侧栏宽度（列）": "sidebar width (columns)",
    "从侧栏恢复历史时不套内存闸门": "no memory gate when resuming history from the sidebar",
    "默认取 atm config": "default from atm config",
    "把某个 pane 换进当前 window 的主格（不进 TUI）": "swap a pane into the main slot of the current window (no TUI)",
    "要换进来的 pane，形如 %%7": "the pane to bring in, e.g. %%7",
    "「当前 pane」是哪个（默认 $TMUX_PANE）": 'which pane is "current" (default $TMUX_PANE)',
    "换进哪一格。默认：从普通格子里跑就是那格自己；从侧栏里跑就是主格（pane_last）": "which slot to swap into. Default: the pane you run from, or the main pane (pane_last) when run from the sidebar",
    "换完把旧格子关掉（默认只有空闲 shell 才自动关；这个会连跑着的进程一起杀）": "kill the old pane after swapping (by default only idle shells are closed; this kills running processes too)",
    "旧格子一律留在后台，空闲 shell 也不关": "always keep the old pane in the background, even an idle shell",
    "把 pane 收进后台窗口 bg（进程继续跑）": "park a pane in the background window bg (the process keeps running)",
    "默认收当前 pane（$TMUX_PANE）": "defaults to the current pane ($TMUX_PANE)",
    "关掉后台窗口 bg 里空闲的 shell 格子（跑着东西的不动）": "close idle shell panes in the background window bg (busy ones are left alone)",
    "只列出来，不关": "list only, do not close",
    "体检：数据源在不在、tmux 通不通": "health check: data sources, tmux, persistence, memory gate",
    "机器可读输出": "machine-readable output",
    "装 tmux 键位绑定（会改 ~/.tmux.conf）": "install tmux key bindings (edits ~/.tmux.conf)",
    "不问直接装": "install without asking",
    "绑哪个键（默认 a）": "key to bind (default a)",
    "侧栏开关键（默认 b；大写 = 把当前格子收进后台）": "sidebar toggle key (default b; uppercase = park the current pane)",
    "浮层宽度": "popup width",
    "浮层高度": "popup height",
    "只看会写什么，不动文件": "show what would be written, touch nothing",
    "tmux 配置文件路径（默认 ~/.tmux.conf）": "tmux config path (default ~/.tmux.conf)",
    "不装 tmux-resurrect / tmux-continuum（默认一起装，重启后能恢复窗口布局）": "skip tmux-resurrect / tmux-continuum (installed by default so layouts survive reboots)",
    "不写总量闸门的 systemd slice（默认写 ~/.config/systemd/user/atm-ai.slice）": "do not write the aggregate systemd slice (default ~/.config/systemd/user/atm-ai.slice)",
    "升级 atm 自己（按安装方式选 uv tool / pipx / pip）": "upgrade atm itself (picks uv tool / pipx / pip based on how it was installed)",
    "只看有没有新版本，不升级": "only check for a newer version",
    "配合 --check：机器可读输出": "with --check: machine-readable output",
    "不问直接升": "upgrade without asking",
    '打印 shell 补全脚本：eval "$(atm completion bash)"': 'print a shell completion script: eval "$(atm completion bash)"',
    "查看 / 修改用户配置（内存闸门参数）。不带参数就是查看": "show / change user config (memory gate settings). No arguments = show",
    "atm config                       查看\natm config memory.high 4G        设置\natm config --unset memory.high   恢复默认\natm config --reset               全部恢复默认": "atm config                       show\natm config memory.high 4G        set\natm config --unset memory.high   reset one key\natm config --reset               reset everything",
    "配置项，如 memory.high": "config key, e.g. memory.high",
    "新值": "new value",
    "把某项恢复默认": "reset one key to its default",
    "全部恢复默认（删掉配置文件）": "reset everything (deletes the config file)",
    "只打印配置文件路径": "print the config file path only",
    "机器可读：每项的值和来源": "machine-readable: every value and where it came from",
    "从 ~/.tmux.conf 里移除 atm 的绑定": "remove atm's bindings from ~/.tmux.conf",
    "没有找到会话。跑 `atm doctor` 看看数据源在不在。": "No sessions found. Run `atm doctor` to check the data sources.",
    "atm pick 需要一个交互终端；非交互场景用 `atm list --json`。": "atm pick needs an interactive terminal; use `atm list --json` in scripts.",
    "没有正在运行的 tmux server。": "No tmux server is running.",
    "atm sidebar 是常驻 TUI，需要交互终端；要开侧栏用 --toggle。": "atm sidebar is a persistent TUI and needs an interactive terminal; use --toggle to open it.",
    "atm swap 需要知道当前 pane：在 tmux 里跑，或加 --pane": "atm swap needs to know the current pane: run inside tmux, or pass --pane",
    "atm park 需要一个 pane id：在 tmux 里跑，或显式传": "atm park needs a pane id: run inside tmux, or pass one explicitly",
    "  tmux: 没装 —— 只能用 --print 模式": "  tmux: not installed — only --print mode works",
    "（配置照样可以先写好，装完 tmux 直接生效。）\n": "(The config can be written now; it takes effect as soon as tmux is installed.)\n",
    "\n这会修改你的全局 tmux 配置（改前会自动备份）。": "\nThis edits your global tmux config (a backup is taken first).",
    "\n这台机器拿不到 cgroup，跳过总量闸门 slice。": "\ncgroups are unavailable on this machine; skipping the aggregate slice.",
    "所有 atm 启动 / 投递的会话共同受这个总量约束；单个会话的上限看 `atm config`。": "Every session launched or dispatched by atm shares this aggregate cap; per-session caps are in `atm config`.",
    "（配置已写好；网络好了在 tmux 里按 prefix + I 让 tpm 补装）": "(Config is written; once online, press prefix + I inside tmux to let tpm install the rest)",
    "持久化在下次起 tmux server 时生效；想现在就要：`tmux source-file ~/.tmux.conf`": "Persistence takes effect the next time the tmux server starts; for right now: `tmux source-file ~/.tmux.conf`",
    "（刻意不自动 source —— 那会重跑你整份配置里带副作用的行）": "(Deliberately not sourced automatically — that would re-run every side-effecting line in your config)",
    "PyPI 最新：查不到（离线或镜像滞后），不影响升级": "PyPI latest: unknown (offline or mirror lag); the upgrade still works",
    "已取消。": "Cancelled.",
    "没找到 atm 写的任何东西（键位块 / 持久化块 / slice），无需移除。": "Nothing written by atm found (key block / persistence block / slice); nothing to remove.",
    "已解绑的键位在下次启动 tmux 时消失（当前 server 可手动 `unbind-key`）。": "Unbound keys disappear the next time tmux starts (run `unbind-key` by hand on the current server).",
    "  cgroup: 不可用（无 systemd user manager 或 memory 控制器未 delegate）—— 一切限制被忽略": "  cgroup: unavailable (no systemd user manager, or the memory controller is not delegated) — every limit is ignored",
    "  已关闭（memory.enabled=false）": "  disabled (memory.enabled=false)",
    "  自动存档钩子: server 没起来，查不了": "  autosave hook: server not running, cannot check",
    "  最近存档: 还没有": "  last save: none yet",
    "只看某个 CLI 的会话": "only sessions from one CLI",
    "只看这个目录（含子目录）下的会话": "only sessions under this directory (recursive)",
    "等价于 --cwd $PWD": "same as --cwd $PWD",
    "强制重新解析，不读缓存": "force a full re-parse, ignore the cache",
    "连机器生成的一次性调用也列出来（默认隐藏，如 `Below is a conversation log…`）": "include machine-generated one-off calls (hidden by default, e.g. `Below is a conversation log…`)",
    "atm pick 需要一个终端来画选择器（/dev/tty 打不开）；非交互场景用 `atm list --json`。": "atm pick needs a terminal to draw the picker (/dev/tty could not be opened); use `atm list --json` in scripts.",
    "atm sidebar --toggle 需要知道当前 pane：在 tmux 里跑，或加 --pane": "atm sidebar --toggle needs to know the current pane: run inside tmux, or pass --pane",
    "已关掉 {v0} 个（{sb_PARK_WINDOW} 空了的话窗口也一起消失）": "Closed {v0} (the {sb_PARK_WINDOW} window disappears too once it is empty)",
    "  Codex 老索引 {v0}: {v1}（{v2} 条标题）": "  Codex legacy index {v0}: {v1} ({v2} titles)",
    "已取消，没有改动任何文件。": "Cancelled, no files were changed.",
    "\n已写入 {result_conf_path}": "\nWritten to {result_conf_path}",
    "（配置已写好，下次起 tmux 或 `prefix + :source-file ~/.tmux.conf` 后生效）": "(Config is written; it takes effect at the next tmux start or after `prefix + :source-file ~/.tmux.conf`)",
    "当前没有运行中的 tmux server；下次 `tmux new` 时自动生效。": "No tmux server is running; it takes effect automatically at the next `tmux new`.",
    "已写入持久化块 → {result_conf_path}": "Persistence block written → {result_conf_path}",
    "当前 {current}（{v0}）": "Current {current} ({v0})",
    "\n将执行：{v0}": "\nWill run: {v0}",
    "完成：{v0}": "Done: {v0}",
    "本来就是默认，没有配置文件": "Already at defaults; there is no config file",
    "  单会话: MemoryHigh={cfg_memory_high} MemoryMax={cfg_memory_max}（atm claude / 投递时套）": "  per session: MemoryHigh={cfg_memory_high} MemoryMax={cfg_memory_max} (applied by atm claude / dispatch)",
    "  配置块: {v0}": "  config block: {v0}",
    "  自动存档钩子: 在 status-right 里，正常": "  autosave hook: present in status-right, OK",
    "  自动存档钩子: ❌ 不在 status-right 里 —— continuum 加载时机器上还有别的 tmux server 就会静默跳过。 关掉多余的 server（含 -L 隔离 socket）后重起主 server": "  autosave hook: ❌ missing from status-right — continuum silently skips it when another tmux server exists at load time. Kill the extra servers (including -L sockets) and restart the main one",
    "  {name} {root}: {status}，{count} 个会话文件": "  {name} {root}: {status}, {count} session files",
    "新分一格：左右分 ┃": "split a new pane: side by side ┃",
    "新分一格：上下分 ━": "split a new pane: top / bottom ━",
    "新开一个 window": "open a new window",
    "只打印命令": "print the command only",
    "不投递": "do not dispatch",
    "... 还有 {v0} 条（-n 调整）": "... {v0} more (adjust with -n)",
    "atm: 找不到会话 {args_session_id}": "atm: no session matches {args_session_id}",
    "atm: {args_session_id} 匹配到 {v0} 条，请给更长的前缀：": "atm: {args_session_id} matches {v0} sessions, give a longer prefix:",
    "{sb_PARK_WINDOW} 里没有空闲的 shell 格子。": "No idle shell panes in {sb_PARK_WINDOW}.",
    "  {v0} {v1}: {status}，{v2} 个会话文件": "  {v0} {v1}: {status}, {v2} session files",
    "  tmux: 已装，server {v0}": "  tmux: installed, server {v0}",
    "  当前在 tmux 里: {v0}": "  inside tmux now: {v0}",
    "tmux 没装。先装：{v0}": "tmux is not installed. Install it first: {v0}",
    "\n注意：PATH 里没找到 `atm`，绑定会用 `{plan_atm_command}`。\n     装成 PATH 命令会更快（仓库根目录 `uv tool install --editable .`）。": "\nNote: `atm` is not on PATH; the bindings will use `{plan_atm_command}`.\n     Installing it as a command is faster (`uv tool install --editable .` in the repo root).",
    "备份    {result_backup_path}": "backup  {result_backup_path}",
    "已对正在运行的 tmux server 立即生效 —— 不用 reload，直接按 prefix + {args_key} 试": "Applied to the running tmux server — no reload needed, try prefix + {args_key}",
    "\n已写总量闸门 {path}（MemoryHigh={st_high} / MemoryMax={st_max}，按物理内存 50% / 65%）": "\nAggregate gate written to {path} (MemoryHigh={st_high} / MemoryMax={st_max}, 50% / 65% of physical RAM)",
    "\n总量闸门 {path} 已存在（MemoryHigh={st_high} / MemoryMax={st_max}），不动。": "\nAggregate gate {path} already exists (MemoryHigh={st_high} / MemoryMax={st_max}); left untouched.",
    "已克隆插件：{v0}": "Cloned plugins: {v0}",
    "克隆 {name} 失败：{err}": "Cloning {name} failed: {err}",
    "（git 装的会跟 main 最新 commit，可能比 PyPI 发行版还新）": "(a git install tracks the latest commit on main and may be newer than the PyPI release)",
    "\n升级命令：{v0}": "\nUpgrade command: {v0}",
    "atm: 升级命令退出码 {result_returncode}": "atm: upgrade command exited with {result_returncode}",
    "atm: PATH 里没有 {program}": "atm: {program} is not on PATH",
    "atm: 找不到 {v0}，本次不套内存闸门": "atm: {v0} not found, running without a memory gate this time",
    "将从 {conf_path} 移除 atm 的绑定块（marker 之外的内容一律不动）。": "Will remove atm's binding block from {conf_path} (nothing outside the markers is touched).",
    "已从 {conf_path} 移除键位块": "Key block removed from {conf_path}",
    "已从 {conf_path} 移除持久化块（~/.tmux/plugins 里已克隆的插件没动，不要了自己 rm）": "Persistence block removed from {conf_path} (cloned plugins in ~/.tmux/plugins are left alone; rm them yourself if unwanted)",
    "已删总量闸门 {slice_name}（只删 atm 自己写的那份）": "Aggregate gate {slice_name} removed (only the one atm wrote)",
    "{prompt} 非交互环境，默认取消。要无人值守请加 --yes。": "{prompt} Non-interactive environment, defaulting to no. Pass --yes for unattended use.",
    "  总量 {st_name}: MemoryHigh={st_high} MemoryMax={st_max}（{who}）": "  aggregate {st_name}: MemoryHigh={st_high} MemoryMax={st_max} ({who})",
    "  总量 {st_name}: ❌ 单元不存在 —— systemd 会建一个无限制的同名 slice，总量闸门形同虚设。跑 atm install 补上": "  aggregate {st_name}: ❌ unit missing — systemd creates an unlimited slice of that name, so the aggregate gate is a no-op. Run atm install to add it",
    "  插件缺: {missing}（在 tmux 里按 prefix + I，或重跑 atm install）": "  plugins missing: {missing} (press prefix + I inside tmux, or rerun atm install)",
    "  插件: {v0} 都在": "  plugins: {v0} all present",
    "  最近存档: {when:%Y-%m-%d %H:%M}": "  last save: {when:%Y-%m-%d %H:%M}",
    "（已隐藏 {filtered_hidden_noise} 条机器生成的一次性调用，--all 可显示）": "({filtered_hidden_noise} machine-generated one-off calls hidden; --all shows them)",
    "按 atm config 的内存闸门启动 {program}（参数原样透传；直接敲 {program} 则不受限）": "launch {program} inside the memory gate from atm config (arguments pass through untouched; a plain {program} is unlimited)",
    "找不到当前 pane {current}": "current pane {current} not found",
    "找不到 pane {target}": "pane {target} not found",
    "对运行中的 server 生效失败：{result_live_error}": "Applying to the running server failed: {result_live_error}",
    "PyPI 最新：{latest}  ← 有新版本": "PyPI latest: {latest}  ← newer version available",
    "PyPI 最新：{latest}  已是最新": "PyPI latest: {latest}  up to date",
    "atm: 找不到 {v0}，请手动升级": "atm: {v0} not found, please upgrade by hand",
    "已删 {path}，全部恢复默认": "Deleted {path}; everything is back to defaults",
    "{args_unset} 已恢复默认 → {path}": "{args_unset} reset to default → {path}",
    "要给 {args_key} 一个值，比如 `atm config {args_key} 4G`": "{args_key} needs a value, e.g. `atm config {args_key} 4G`",
    "备份 {backup}": "backup {backup}",
    "备份 {backup_persist}": "backup {backup_persist}",
    "不支持的 shell {shell!r}，可选 bash / zsh / fish": "unsupported shell {shell!r}; choose bash / zsh / fish",
    "⚠ 这台机器拿不到 cgroup（无 systemd user manager 或 memory 控制器未 delegate），": "⚠ cgroups are unavailable on this machine (no systemd user manager, or the memory controller is not delegated),",
    "  以上设置会被静默忽略，atm claude 等价于直接跑 claude。": "  so the settings above are silently ignored and atm claude is the same as plain claude.",
    "{label} 要形如 4G / 512M / infinity，收到 {value!r}": "{label} must look like 4G / 512M / infinity, got {value!r}",
    "不认识的配置项 {key!r}，可用：{v0}": "unknown config key {key!r}; available: {v0}",
    "{key} 要 true/false，收到 {value!r}": "{key} must be true/false, got {value!r}",
    "{p} 读不了：{exc}": "cannot read {p}: {exc}",
    "{p}：不认识的段 [{section}]，只有 {v0}": "{p}: unknown section [{section}]; only {v0}",
    "{p}：[{section}] 应该是一个表，收到 {v0}": "{p}: [{section}] must be a table, got {v0}",
    "{key} 要形如 name.slice，收到 {value!r}": "{key} must look like name.slice, got {value!r}",
    "{p}：[{section}] 里不认识 {name!r}，可用：{v0}": "{p}: unknown key {name!r} in [{section}]; available: {v0}",
    "没有正在运行的 tmux server —— 先 `tmux new -s main`，或用 --print": "No tmux server is running — start one with `tmux new -s main`, or use --print",
    "找不到 pane {pane_id}": "pane {pane_id} not found",
    "不认识的会话来源: {entry_source}": "unknown session source: {entry_source}",
    "这条会话的工作目录已经不存在了：{entry_cwd}\n        投递会执行 `cd` 到该目录，失败后整条命令会中止、claude 不会启动。\n        （常见原因：/tmp 下的 scratchpad 被清理、git worktree 被删、项目目录移动过）": "This session's working directory no longer exists: {entry_cwd}\n        Dispatch runs `cd` into it first; when that fails the whole command aborts and claude never starts.\n        (Common causes: a scratchpad under /tmp was cleaned, a git worktree was removed, the project moved)",
    "投递到已有 pane 需要 pane_id": "dispatching into an existing pane needs a pane_id",
    "不认识的投递目标: {target_kind}": "unknown dispatch target: {target_kind}",
    "绑定说明：": "Bindings:",
    "（已存在 atm 配置块，会被整块替换掉，不会重复追加）": "(An atm block already exists; it is replaced as a whole, never appended twice)",
    "全部会话": "all sessions",
    "只看当前目录的会话": "sessions from the current directory only",
    "侧栏：开 / 切过去 / 收起": "sidebar: open / switch to / collapse",
    "把当前格子收进后台窗口 bg": "park the current pane in the background window bg",
    "{path} 的备份文件名连撞 100 次，放弃。": "Backup names for {path} collided 100 times in a row; giving up.",
    "--key 和 --sidebar-key 不能相同（都是 {key!r}）": "--key and --sidebar-key must differ (both are {key!r})",
    "{begin} 有开始标记但找不到对应的 {end}。为避免误删标记之后的配置，这里不做任何改动 —— 请手动检查 ~/.tmux.conf。": "Found the {begin} marker but no matching {end}. To avoid deleting config after the marker, nothing was changed — please inspect ~/.tmux.conf by hand.",
    "{name} 只能是单个小写字母，收到 {value!r}。（配套的第二条绑定靠 .upper() 区分，非字母的 upper() 是恒等变换，两条绑定会撞成同一个键、第一条静默失效。）": "{name} must be a single lowercase letter, got {value!r}. (The paired second binding relies on .upper(); for non-letters upper() is a no-op, so both bindings land on the same key and the first one silently dies.)",
    "{path} 存在但读不出来（{v0}）。为避免覆盖掉你现有的配置，这里直接中止。请先确认它的编码/权限，或手动备份后删除它。": "{path} exists but cannot be read ({v0}). Aborting rather than overwrite your existing config. Check its encoding / permissions, or back it up and delete it.",
    "备份 {path} 到 {backup} 失败（{v0}: {exc}）。为避免没有退路地改你的配置，这里直接中止。": "Backing up {path} to {backup} failed ({v0}: {exc}). Aborting rather than edit your config with no way back.",
    "没找到 git": "git not found",
    "（已存在持久化块，会被整块替换掉，不会重复追加）": "(A persistence block already exists; it is replaced as a whole, never appended twice)",
    "将写入 {self_conf_path}（持久化块）：": "Will write to {self_conf_path} (persistence block):",
    "  ⚠ 没找到 git，克隆会跳过；装好 git 后重跑，或在 tmux 里按 prefix + I": "  ⚠ git not found, cloning will be skipped; rerun after installing git, or press prefix + I inside tmux",
    "{self_conf_path} 里已经有你自己的 tpm 配置，不重复写插件块；要恢复功能请自行加上 tmux-resurrect / tmux-continuum。": "{self_conf_path} already has your own tpm setup; the plugin block is not written. Add tmux-resurrect / tmux-continuum yourself for restore.",
    "将 git clone 到 {self_plugins_dir}/：{names}": "Will git clone into {self_plugins_dir}/: {names}",
    "插件已在 {self_plugins_dir}/，不重复克隆": "Plugins already in {self_plugins_dir}/; not cloning again",
    "那是侧栏自己": "that is the sidebar itself",
    "本窗口除了侧栏没有别的格子，没法换位 —— 先分一格出来": "this window has no pane besides the sidebar, nothing to swap with — split one off first",
    "侧栏自己不能收进后台 —— 要收起侧栏用 `atm sidebar --toggle`": "the sidebar itself cannot be parked — collapse it with `atm sidebar --toggle`",
    "不能换进侧栏的位置": "cannot swap into the sidebar's slot",
    "找不到 pane {target_id}（可能刚被关掉）": "pane {target_id} not found (it may have just been closed)",
    "pane {pane_id} 已经在 {PARK_WINDOW} 里了": "pane {pane_id} is already in {PARK_WINDOW}",
    "pane {pane_id} 已经独占一个窗口，本来就在后台，不用收": "pane {pane_id} already owns a window of its own — it is in the background as it is",
    "找不到当前 pane {current_pane}": "current pane {current_pane} not found",
    "找不到目标格子 {slot_id}": "target slot {slot_id} not found",
    "atm sidebar 必须在 tmux pane 里跑（用 `atm sidebar --toggle` 打开）": "atm sidebar must run inside a tmux pane (open it with `atm sidebar --toggle`)",
    "tmux 没装或不在 PATH 里": "tmux is not installed or not on PATH",
    "tmux {v0} 失败: {detail}": "tmux {v0} failed: {detail}",
    "tmux {v0} 超时": "tmux {v0} timed out",
    "无法执行 tmux: {exc}": "cannot run tmux: {exc}",
    "⚠目录已删 ": "⚠dir gone ",
    "要不要套闸门（true/false）": "whether to apply the gate at all (true/false)",
    "软上限：超了节流 + 回收，不杀（如 4G）": "soft cap: throttle + reclaim above it, never kills (e.g. 4G)",
    "硬上限：回收压不住才杀，杀的是整个会话及其子进程（如 8G）": "hard cap: kills when reclaim can't keep up — the whole session and its children (e.g. 8G)",
    "swap 上限（WSL 上 swap 在宿主 SSD，建议小）": "swap cap (on WSL swap lives on the host SSD; keep it small)",
    "所有会话共同归入的 systemd slice，兜总量": "systemd slice every session joins; caps the total",
    "systemd-run 是否走 --user（一般 true）": "whether systemd-run uses --user (normally true)",
    "存在": "present",
    "不存在": "missing",
    "atm 写的": "written by atm",
    "用户自己的": "the user's own",
    "你自己的": "your own",
    " ←当前": " ←current",
    "把「{v0}」{suffix} 投到哪里？": 'Where should "{v0}"{suffix} go?',
    "继续吗？": "Continue?",
    "有": "yes",
    "无": "no",
    "（跑 atm --version 看版本）": "(run atm --version to see the version)",
    "已装": "installed",
    "未装（atm install 会一起装）": "not installed (atm install adds it)",
    "在跑": "running",
    "没起来": "not running",
    "是": "yes",
    "否": "no",
    "# atm 的用户配置。改完立即生效，不用重启。": "# atm user config. Changes take effect immediately, no restart needed.",
    "# `atm config` 查看，`atm config <key> <value>` 修改。": "# `atm config` shows it, `atm config <key> <value>` changes it.",
    "配置文件：{v0}{v1}": "Config file: {v0}{v1}",
    "（不存在，全部默认）": " (does not exist, everything at defaults)",
    "优先级：命令行参数 > 环境变量（{ENV_PREFIX}MEMORY_HIGH 这类）> 文件 > 默认": "Precedence: flags > environment ({ENV_PREFIX}MEMORY_HIGH etc.) > file > default",
    "提示：这条会话的转录有 {mb:.0f}MB，{v0} 可能会先问你要不要「从摘要恢复」。": "Note: this session's transcript is {mb:.0f}MB; {v0} may first ask whether to resume from a summary.",
    "新开": "Opened",
    "投递到": "Dispatched to",
    "pane {pane_id} ({pane_label}) 里跑的是 `{pane_current_command}`，它的语法和我们生成的 POSIX 命令行（`cd '…' && …`，shlex 单引号转义）不兼容 —— 投进去不会报错，会**执行成别的东西**。用 --force 强制投递。": "pane {pane_id} ({pane_label}) is running `{pane_current_command}`, whose syntax is incompatible with the POSIX command line we generate (`cd '…' && …`, shlex single-quote escaping) — it would not error, it would **run as something else**. Use --force to dispatch anyway.",
    "pane {pane_id} ({pane_label}) 里正在跑 `{pane_current_command}`，不是空闲 shell —— 投进去会被当成那个程序的输入。用 --force 强制投递。": "pane {pane_id} ({pane_label}) is running `{pane_current_command}`, not an idle shell — the command would become that program's input. Use --force to dispatch anyway.",
    "将写入总量闸门 {st_path}：MemoryHigh={high} / MemoryMax={max_}（物理内存 {v0}G 的 50% / 65%），然后 systemctl --user daemon-reload。": "Will write the aggregate gate {st_path}: MemoryHigh={high} / MemoryMax={max_} (50% / 65% of {v0}G physical RAM), then systemctl --user daemon-reload.",
    "# 软限：超了强回收 + 节流，不杀。": "# Soft cap: reclaim + throttle above it, never kills.",
    "# 硬限：到这里才杀。宁可掉一个会话，也不要整机冻死。": "# Hard cap: kills at this point. Better to lose one session than freeze the whole machine.",
    "总量闸门 {st_path} 已存在（MemoryHigh={st_high} / MemoryMax={st_max}，{who}），不动。": "Aggregate gate {st_path} already exists (MemoryHigh={st_high} / MemoryMax={st_max}, {who}); left untouched.",
    "将写入 {self_conf_path}：": "Will write to {self_conf_path}:",
    "用你的包管理器装 tmux（>= 3.2，需要 display-popup）": "install tmux with your package manager (>= 3.2, display-popup is required)",
    "# 重启后恢复 session / window / pane / cwd（只搭骨架，不重新拉起 claude / codex ——": "# Restore session / window / pane / cwd after reboot (skeleton only, does not relaunch claude / codex —",
    "# 会话在对应格子里用 atm 按需 resume，别把它们加进 @resurrect-processes）": "# resume sessions on demand with atm in the right pane; do not add them to @resurrect-processes)",
    "git clone {name} 失败": "git clone {name} failed",
    "，旧格子是空闲 shell，已关掉": ", the old pane was an idle shell and was closed",
    "，旧格子进后台": ", the old pane went to the background",
    "并入": "joined",
    "新建": "created",
    "把 {self_target} 换进 {self_slot} 的位置{tail}": "Swapped {self_target} into {self_slot}'s slot{tail}",
    "把 {self_pane} 收进后台窗口 {PARK_WINDOW}（{verb}）": "Parked {self_pane} in background window {PARK_WINDOW} ({verb})",
    "打开侧栏": "Sidebar opened",
    "切到 {self_target}（已在本窗口）": "Switched to {self_target} (already in this window)",
    "切到侧栏 {self_sidebar_id}": "Switched to sidebar {self_sidebar_id}",
    "收起侧栏 {self_sidebar_id}": "Sidebar {self_sidebar_id} collapsed",
    "⏎换入 ^T选格 ^X收后台 Tab来源 ^R重建 ^C退": "⏎swap ^Tslot ^Xpark Tab src ^Rrebuild ^Cquit",
    "历史": "history",
    "数字/⏎ 选格  Esc 取消": "digit/⏎ pick slot  Esc cancel",
    "本窗口除了侧栏没有别的格子": "no pane in this window besides the sidebar",
    "^X 只对运行中的格子有效": "^X only works on running panes",
    "已恢复 → {result_pane_id}": "Resumed → {result_pane_id}",
    "换进哪格？(数字=prefix+q 编号)": "Swap into which slot? (digit = prefix+q number)",
    "本窗口没有 {pane_index} 号格子": "this window has no pane #{pane_index}",
    "运行中 {v0}": "running {v0}",
    "把「{v0}」换进…": 'Swap "{v0}" into…',
    "已重建索引": "index rebuilt",
    "↑↓/^N^P 移动  Tab 切来源  ^G 切聚合(目录/agent/平铺)  Enter 选中  Esc 取消": "↑↓/^N^P move  Tab source  ^G grouping (dir/agent/flat)  Enter select  Esc cancel",
    "↑↓ 移动  数字键直选  Enter 确认  Esc 取消": "↑↓ move  digits pick  Enter confirm  Esc cancel",
    "  ⚠ resume 会让 CLI 读完整个文件": "  ⚠ resume makes the CLI read the whole file",
    "⚠ 工作目录已不存在  ": "⚠ working directory is gone  ",
    "↑↓ 移动({first}-{last}/{total})  数字键直选  Enter 确认  Esc 取消": "↑↓ move ({first}-{last}/{total})  digits pick  Enter confirm  Esc cancel",
    "源码目录": "the source directory",
    "认不出这份 atm 是怎么装的。手动升级：\n  uv tool upgrade {DIST_NAME}        # uv tool 装的\n  pipx upgrade {DIST_NAME}           # pipx 装的\n  pip install -U {DIST_NAME}         # pip 装的（注意 PyPI 上的 `atm` 是别人的包）": "Cannot tell how this atm was installed. Upgrade by hand:\n  uv tool upgrade {DIST_NAME}        # installed with uv tool\n  pipx upgrade {DIST_NAME}           # installed with pipx\n  pip install -U {DIST_NAME}         # installed with pip (note: `atm` on PyPI is someone else's package)",
    "这是 editable 安装（{where}），升级就是 `git pull`；改了依赖再 `uv sync --frozen`。": "This is an editable install ({where}); upgrading means `git pull`, then `uv sync --frozen` if dependencies changed.",
    "不在 PATH 里 —— 投递出去会失败": "not on PATH — dispatch will fail",
}

JA: dict[str, str] = {
    "== 数据源 ==": "== データソース ==",
    "\n== 持久化（resurrect + continuum）==": "\n== 永続化（resurrect + continuum）==",
    "\n== 内存闸门 ==": "\n== メモリゲート ==",
    "\n== 索引 ==": "\n== インデックス ==",
    "手动存/恢复：prefix + Ctrl-s / prefix + Ctrl-r；": "手動保存 / 復元：prefix + Ctrl-s / prefix + Ctrl-r；",
    "自动存档每 10 分钟，只在有客户端 attach 时触发": "自動保存は 10 分ごと、クライアントが attach しているときだけ",
    "  直接敲 claude / codex 不受以上任何限制；要限就用 atm claude / atm codex": "  素の claude / codex は上記の制限を受けない；制限したいなら atm claude / atm codex",
    "AI Terminal Manager — Claude Code / Codex / Pi 的统一会话历史，投到指定 tmux pane": "AI Terminal Manager — Claude Code / Codex / Pi のセッション履歴を統合し、任意の tmux pane に投入",
    "多打点过程信息到 stderr（-v 概要，-vv 逐文件/逐条 tmux 命令；ATM_DEBUG=1 同 -vv）": "stderr に進行情報を増やす（-v 概要、-vv ファイル単位 / tmux コマンド単位；ATM_DEBUG=1 は -vv と同じ）",
    "列出会话": "セッションを一覧",
    "最多列几条（默认 30）": "最大表示件数（デフォルト 30）",
    "输出 JSON": "JSON で出力",
    "交互选一条会话并投递（主命令）": "対話的にセッションを選んで投入（メインコマンド）",
    "预填搜索词": "検索語を事前入力",
    "列表怎么聚合：cwd(目录，默认) / agent / none(平铺)。TUI 里 Ctrl-G 可循环切换": "グループ化：cwd（ディレクトリ、デフォルト）/ agent / none（フラット）。TUI 内は Ctrl-G で切替",
    "直接投到这个 pane（跳过选目标那一步）": "この pane に直接投入（対象選択をスキップ）",
    "投到新分出来的 pane": "新しく分割した pane に投入",
    "投到新 window": "新しい window に投入",
    "--split 时上下分而不是左右分": "--split 時に左右ではなく上下に分割",
    "只打印命令，不投递": "コマンドを表示するだけで投入しない",
    "目标 pane 正忙也照投": "対象 pane が忙しくても投入する",
    "不给会话套 cgroup 内存闸门（默认套）": "セッションに cgroup メモリゲートを掛けない（デフォルトは掛ける）",
    "软上限，超了节流+回收不杀（默认取 atm config）": "ソフト上限：超えたらスロットリング + 回収、殺さない（デフォルトは atm config）",
    "硬上限，回收压不住才杀（默认取 atm config）": "ハード上限：回収で抑えられないときだけ kill（デフォルトは atm config）",
    "按 id 直接投递（不进 TUI）": "id 指定で直接投入（TUI なし）",
    "会话 id（可以只写前缀）": "セッション id（前方一致で可）",
    "投到这个 pane": "この pane に投入",
    "从当前 pane 分一格出来投": "現在の pane から一つ分割して投入",
    "新开 window 投": "新しい window を開いて投入",
    "只打印命令，不投": "コマンドを表示するだけ",
    "配合 --split：上下分而不是左右分": "--split と併用：左右ではなく上下に分割",
    "构建/查看索引": "インデックスの構築 / 確認",
    "清缓存后全量重建": "キャッシュを消して全再構築",
    "列出 tmux pane": "tmux pane を一覧",
    "常驻侧栏：列出运行中的格子和历史，选中换进主格": "常駐サイドバー：実行中の pane と履歴を一覧、選ぶとメインに入れ替え",
    "开/切/收侧栏：没开就在当前 window 最左边开一个；开了就切过去；焦点在侧栏上就收起": "サイドバーを 開/移動/閉：閉じていれば現在の window の最左に開く；開いていれば移動；フォーカス中なら閉じる",
    "--toggle 时「当前 pane」是哪个（键位绑定里传 #{pane_id}）": "--toggle 時の「現在の pane」（キーバインドから #{pane_id} を渡す）",
    "侧栏宽度（列）": "サイドバーの幅（桁）",
    "从侧栏恢复历史时不套内存闸门": "サイドバーから履歴を復元するときメモリゲートを掛けない",
    "默认取 atm config": "デフォルトは atm config",
    "把某个 pane 换进当前 window 的主格（不进 TUI）": "pane を現在の window のメイン枠に入れ替え（TUI なし）",
    "要换进来的 pane，形如 %%7": "入れ替える pane、例 %%7",
    "「当前 pane」是哪个（默认 $TMUX_PANE）": "「現在の pane」（デフォルト $TMUX_PANE）",
    "换进哪一格。默认：从普通格子里跑就是那格自己；从侧栏里跑就是主格（pane_last）": "どの枠に入れるか。デフォルト：通常の pane からならその pane、サイドバーからならメイン（pane_last）",
    "换完把旧格子关掉（默认只有空闲 shell 才自动关；这个会连跑着的进程一起杀）": "入れ替え後に旧 pane を閉じる（デフォルトは idle な shell だけ自動で閉じる；これは実行中のプロセスも kill）",
    "旧格子一律留在后台，空闲 shell 也不关": "旧 pane は常にバックグラウンドに残す、idle な shell も閉じない",
    "把 pane 收进后台窗口 bg（进程继续跑）": "pane をバックグラウンド window bg に退避（プロセスは継続）",
    "默认收当前 pane（$TMUX_PANE）": "デフォルトは現在の pane（$TMUX_PANE）",
    "关掉后台窗口 bg 里空闲的 shell 格子（跑着东西的不动）": "バックグラウンド window bg の idle な shell pane を閉じる（実行中のものは触らない）",
    "只列出来，不关": "一覧だけ、閉じない",
    "体检：数据源在不在、tmux 通不通": "健診：データソース、tmux、永続化、メモリゲート",
    "机器可读输出": "機械可読な出力",
    "装 tmux 键位绑定（会改 ~/.tmux.conf）": "tmux キーバインドを導入（~/.tmux.conf を編集）",
    "不问直接装": "確認せずに導入",
    "绑哪个键（默认 a）": "バインドするキー（デフォルト a）",
    "侧栏开关键（默认 b；大写 = 把当前格子收进后台）": "サイドバー切替キー（デフォルト b；大文字 = 現在の pane を退避）",
    "浮层宽度": "ポップアップの幅",
    "浮层高度": "ポップアップの高さ",
    "只看会写什么，不动文件": "書き込む内容を表示するだけ、ファイルは触らない",
    "tmux 配置文件路径（默认 ~/.tmux.conf）": "tmux 設定ファイルのパス（デフォルト ~/.tmux.conf）",
    "不装 tmux-resurrect / tmux-continuum（默认一起装，重启后能恢复窗口布局）": "tmux-resurrect / tmux-continuum を入れない（デフォルトは一緒に導入、再起動後にレイアウトが戻る）",
    "不写总量闸门的 systemd slice（默认写 ~/.config/systemd/user/atm-ai.slice）": "合計ゲートの systemd slice を書かない（デフォルトは ~/.config/systemd/user/atm-ai.slice）",
    "升级 atm 自己（按安装方式选 uv tool / pipx / pip）": "atm 自身を更新（インストール方法に応じて uv tool / pipx / pip）",
    "只看有没有新版本，不升级": "新版があるか確認するだけ",
    "配合 --check：机器可读输出": "--check と併用：機械可読な出力",
    "不问直接升": "確認せずに更新",
    '打印 shell 补全脚本：eval "$(atm completion bash)"': 'シェル補完スクリプトを出力：eval "$(atm completion bash)"',
    "查看 / 修改用户配置（内存闸门参数）。不带参数就是查看": "ユーザー設定（メモリゲート）の表示 / 変更。引数なしで表示",
    "atm config                       查看\natm config memory.high 4G        设置\natm config --unset memory.high   恢复默认\natm config --reset               全部恢复默认": "atm config                       表示\natm config memory.high 4G        設定\natm config --unset memory.high   一項目をデフォルトに\natm config --reset               すべてデフォルトに",
    "配置项，如 memory.high": "設定キー、例 memory.high",
    "新值": "新しい値",
    "把某项恢复默认": "一項目をデフォルトに戻す",
    "全部恢复默认（删掉配置文件）": "すべてデフォルトに（設定ファイルを削除）",
    "只打印配置文件路径": "設定ファイルのパスだけ表示",
    "机器可读：每项的值和来源": "機械可読：各項目の値と由来",
    "从 ~/.tmux.conf 里移除 atm 的绑定": "~/.tmux.conf から atm のバインドを削除",
    "没有找到会话。跑 `atm doctor` 看看数据源在不在。": "セッションが見つかりません。`atm doctor` でデータソースを確認してください。",
    "atm pick 需要一个交互终端；非交互场景用 `atm list --json`。": "atm pick には対話端末が必要です；スクリプトでは `atm list --json` を。",
    "没有正在运行的 tmux server。": "実行中の tmux server がありません。",
    "atm sidebar 是常驻 TUI，需要交互终端；要开侧栏用 --toggle。": "atm sidebar は常駐 TUI で対話端末が必要です；開くには --toggle。",
    "atm swap 需要知道当前 pane：在 tmux 里跑，或加 --pane": "atm swap は現在の pane を知る必要があります：tmux 内で実行するか --pane を",
    "atm park 需要一个 pane id：在 tmux 里跑，或显式传": "atm park には pane id が必要です：tmux 内で実行するか明示的に渡す",
    "  tmux: 没装 —— 只能用 --print 模式": "  tmux: 未インストール —— --print モードのみ",
    "（配置照样可以先写好，装完 tmux 直接生效。）\n": "（設定は先に書けます；tmux を入れれば即有効。）\n",
    "\n这会修改你的全局 tmux 配置（改前会自动备份）。": "\nグローバルな tmux 設定を変更します（事前に自動バックアップ）。",
    "\n这台机器拿不到 cgroup，跳过总量闸门 slice。": "\nこのマシンでは cgroup が使えないため、合計ゲートの slice をスキップします。",
    "所有 atm 启动 / 投递的会话共同受这个总量约束；单个会话的上限看 `atm config`。": "atm が起動 / 投入した全セッションがこの合計上限を共有します；セッション単位の上限は `atm config`。",
    "（配置已写好；网络好了在 tmux 里按 prefix + I 让 tpm 补装）": "（設定は書きました；オンラインになったら tmux 内で prefix + I を押して tpm に入れさせてください）",
    "持久化在下次起 tmux server 时生效；想现在就要：`tmux source-file ~/.tmux.conf`": "永続化は次に tmux server が起動したときに有効；今すぐなら `tmux source-file ~/.tmux.conf`",
    "（刻意不自动 source —— 那会重跑你整份配置里带副作用的行）": "（意図的に自動 source しません —— 設定内の副作用のある行を再実行してしまうため）",
    "PyPI 最新：查不到（离线或镜像滞后），不影响升级": "PyPI 最新：取得できず（オフラインまたはミラー遅延）、更新には影響なし",
    "已取消。": "キャンセルしました。",
    "没找到 atm 写的任何东西（键位块 / 持久化块 / slice），无需移除。": "atm が書いたものは見つかりません（キーブロック / 永続化ブロック / slice）；削除するものなし。",
    "已解绑的键位在下次启动 tmux 时消失（当前 server 可手动 `unbind-key`）。": "解除したキーは次回 tmux 起動時に消えます（現在の server では手動で `unbind-key`）。",
    "  cgroup: 不可用（无 systemd user manager 或 memory 控制器未 delegate）—— 一切限制被忽略": "  cgroup: 使用不可（systemd user manager なし、または memory コントローラ未委譲）—— すべての制限が無視されます",
    "  已关闭（memory.enabled=false）": "  無効（memory.enabled=false）",
    "  自动存档钩子: server 没起来，查不了": "  自動保存フック: server が起動していないため確認不可",
    "  最近存档: 还没有": "  最新の保存: まだなし",
    "只看某个 CLI 的会话": "特定の CLI のセッションのみ",
    "只看这个目录（含子目录）下的会话": "このディレクトリ配下（サブディレクトリ含む）のセッションのみ",
    "等价于 --cwd $PWD": "--cwd $PWD と同じ",
    "强制重新解析，不读缓存": "キャッシュを使わず強制再解析",
    "连机器生成的一次性调用也列出来（默认隐藏，如 `Below is a conversation log…`）": "機械生成の一回限りの呼び出しも表示（デフォルトは非表示、例 `Below is a conversation log…`）",
    "atm pick 需要一个终端来画选择器（/dev/tty 打不开）；非交互场景用 `atm list --json`。": "atm pick はピッカーを描く端末が必要です（/dev/tty を開けません）；スクリプトでは `atm list --json` を。",
    "atm sidebar --toggle 需要知道当前 pane：在 tmux 里跑，或加 --pane": "atm sidebar --toggle は現在の pane を知る必要があります：tmux 内で実行するか --pane を",
    "已关掉 {v0} 个（{sb_PARK_WINDOW} 空了的话窗口也一起消失）": "{v0} 個を閉じました（{sb_PARK_WINDOW} が空になれば window も消えます）",
    "  Codex 老索引 {v0}: {v1}（{v2} 条标题）": "  Codex 旧インデックス {v0}: {v1}（タイトル {v2} 件）",
    "已取消，没有改动任何文件。": "キャンセルしました。ファイルは変更していません。",
    "\n已写入 {result_conf_path}": "\n{result_conf_path} に書き込みました",
    "（配置已写好，下次起 tmux 或 `prefix + :source-file ~/.tmux.conf` 后生效）": "（設定は書きました；次回の tmux 起動時、または `prefix + :source-file ~/.tmux.conf` 後に有効）",
    "当前没有运行中的 tmux server；下次 `tmux new` 时自动生效。": "実行中の tmux server がありません；次の `tmux new` で自動的に有効になります。",
    "已写入持久化块 → {result_conf_path}": "永続化ブロックを書き込みました → {result_conf_path}",
    "当前 {current}（{v0}）": "現在 {current}（{v0}）",
    "\n将执行：{v0}": "\n実行します：{v0}",
    "完成：{v0}": "完了：{v0}",
    "本来就是默认，没有配置文件": "すでにデフォルトです。設定ファイルはありません",
    "  单会话: MemoryHigh={cfg_memory_high} MemoryMax={cfg_memory_max}（atm claude / 投递时套）": "  セッション単位: MemoryHigh={cfg_memory_high} MemoryMax={cfg_memory_max}（atm claude / 投入時に適用）",
    "  配置块: {v0}": "  設定ブロック: {v0}",
    "  自动存档钩子: 在 status-right 里，正常": "  自動保存フック: status-right にあり、正常",
    "  自动存档钩子: ❌ 不在 status-right 里 —— continuum 加载时机器上还有别的 tmux server 就会静默跳过。 关掉多余的 server（含 -L 隔离 socket）后重起主 server": "  自動保存フック: ❌ status-right にありません —— continuum はロード時に別の tmux server があると黙ってスキップします。余分な server（-L の分離 socket 含む）を止めてメイン server を再起動してください",
    "  {name} {root}: {status}，{count} 个会话文件": "  {name} {root}: {status}、セッションファイル {count} 件",
    "新分一格：左右分 ┃": "新しく分割：左右 ┃",
    "新分一格：上下分 ━": "新しく分割：上下 ━",
    "新开一个 window": "新しい window を開く",
    "只打印命令": "コマンドを表示するだけ",
    "不投递": "投入しない",
    "... 还有 {v0} 条（-n 调整）": "... 他 {v0} 件（-n で調整）",
    "atm: 找不到会话 {args_session_id}": "atm: セッション {args_session_id} が見つかりません",
    "atm: {args_session_id} 匹配到 {v0} 条，请给更长的前缀：": "atm: {args_session_id} は {v0} 件に一致します。もっと長い前方一致を：",
    "{sb_PARK_WINDOW} 里没有空闲的 shell 格子。": "{sb_PARK_WINDOW} に idle な shell pane はありません。",
    "  {v0} {v1}: {status}，{v2} 个会话文件": "  {v0} {v1}: {status}、セッションファイル {v2} 件",
    "  tmux: 已装，server {v0}": "  tmux: インストール済み、server {v0}",
    "  当前在 tmux 里: {v0}": "  現在 tmux 内: {v0}",
    "tmux 没装。先装：{v0}": "tmux が未インストールです。先に：{v0}",
    "\n注意：PATH 里没找到 `atm`，绑定会用 `{plan_atm_command}`。\n     装成 PATH 命令会更快（仓库根目录 `uv tool install --editable .`）。": "\n注意：PATH に `atm` がありません。バインドは `{plan_atm_command}` を使います。\n     コマンドとしてインストールした方が速いです（リポジトリ直下で `uv tool install --editable .`）。",
    "备份    {result_backup_path}": "バックアップ {result_backup_path}",
    "已对正在运行的 tmux server 立即生效 —— 不用 reload，直接按 prefix + {args_key} 试": "実行中の tmux server に即時反映しました —— reload 不要、prefix + {args_key} を試してください",
    "\n已写总量闸门 {path}（MemoryHigh={st_high} / MemoryMax={st_max}，按物理内存 50% / 65%）": "\n合計ゲートを {path} に書きました（MemoryHigh={st_high} / MemoryMax={st_max}、物理メモリの 50% / 65%）",
    "\n总量闸门 {path} 已存在（MemoryHigh={st_high} / MemoryMax={st_max}），不动。": "\n合計ゲート {path} は既に存在します（MemoryHigh={st_high} / MemoryMax={st_max}）。触りません。",
    "已克隆插件：{v0}": "プラグインを clone しました：{v0}",
    "克隆 {name} 失败：{err}": "{name} の clone に失敗：{err}",
    "（git 装的会跟 main 最新 commit，可能比 PyPI 发行版还新）": "（git インストールは main の最新 commit を追うため、PyPI のリリースより新しいことがあります）",
    "\n升级命令：{v0}": "\n更新コマンド：{v0}",
    "atm: 升级命令退出码 {result_returncode}": "atm: 更新コマンドの終了コード {result_returncode}",
    "atm: PATH 里没有 {program}": "atm: PATH に {program} がありません",
    "atm: 找不到 {v0}，本次不套内存闸门": "atm: {v0} が見つからないため、今回はメモリゲートなしで実行します",
    "将从 {conf_path} 移除 atm 的绑定块（marker 之外的内容一律不动）。": "{conf_path} から atm のバインドブロックを削除します（マーカー外は一切触りません）。",
    "已从 {conf_path} 移除键位块": "{conf_path} からキーブロックを削除しました",
    "已从 {conf_path} 移除持久化块（~/.tmux/plugins 里已克隆的插件没动，不要了自己 rm）": "{conf_path} から永続化ブロックを削除しました（~/.tmux/plugins の clone 済みプラグインは残しています；不要なら rm してください）",
    "已删总量闸门 {slice_name}（只删 atm 自己写的那份）": "合計ゲート {slice_name} を削除しました（atm が書いたものだけ）",
    "{prompt} 非交互环境，默认取消。要无人值守请加 --yes。": "{prompt} 非対話環境のためキャンセル扱いです。無人実行には --yes を。",
    "  总量 {st_name}: MemoryHigh={st_high} MemoryMax={st_max}（{who}）": "  合計 {st_name}: MemoryHigh={st_high} MemoryMax={st_max}（{who}）",
    "  总量 {st_name}: ❌ 单元不存在 —— systemd 会建一个无限制的同名 slice，总量闸门形同虚设。跑 atm install 补上": "  合計 {st_name}: ❌ ユニットがありません —— systemd は無制限の同名 slice を作るため、合計ゲートは無効同然です。atm install で追加してください",
    "  插件缺: {missing}（在 tmux 里按 prefix + I，或重跑 atm install）": "  プラグイン不足: {missing}（tmux 内で prefix + I、または atm install を再実行）",
    "  插件: {v0} 都在": "  プラグイン: {v0} すべてあり",
    "  最近存档: {when:%Y-%m-%d %H:%M}": "  最新の保存: {when:%Y-%m-%d %H:%M}",
    "（已隐藏 {filtered_hidden_noise} 条机器生成的一次性调用，--all 可显示）": "（機械生成の一回限りの呼び出し {filtered_hidden_noise} 件を非表示；--all で表示）",
    "按 atm config 的内存闸门启动 {program}（参数原样透传；直接敲 {program} 则不受限）": "atm config のメモリゲート内で {program} を起動（引数はそのまま透過；素の {program} は無制限）",
    "找不到当前 pane {current}": "現在の pane {current} が見つかりません",
    "找不到 pane {target}": "pane {target} が見つかりません",
    "对运行中的 server 生效失败：{result_live_error}": "実行中の server への反映に失敗：{result_live_error}",
    "PyPI 最新：{latest}  ← 有新版本": "PyPI 最新：{latest}  ← 新版があります",
    "PyPI 最新：{latest}  已是最新": "PyPI 最新：{latest}  最新です",
    "atm: 找不到 {v0}，请手动升级": "atm: {v0} が見つかりません。手動で更新してください",
    "已删 {path}，全部恢复默认": "{path} を削除し、すべてデフォルトに戻しました",
    "{args_unset} 已恢复默认 → {path}": "{args_unset} をデフォルトに戻しました → {path}",
    "要给 {args_key} 一个值，比如 `atm config {args_key} 4G`": "{args_key} には値が必要です。例：`atm config {args_key} 4G`",
    "备份 {backup}": "バックアップ {backup}",
    "备份 {backup_persist}": "バックアップ {backup_persist}",
    "不支持的 shell {shell!r}，可选 bash / zsh / fish": "未対応のシェル {shell!r}。bash / zsh / fish から選択",
    "⚠ 这台机器拿不到 cgroup（无 systemd user manager 或 memory 控制器未 delegate），": "⚠ このマシンでは cgroup が使えません（systemd user manager なし、または memory コントローラ未委譲）。",
    "  以上设置会被静默忽略，atm claude 等价于直接跑 claude。": "  上記の設定は黙って無視され、atm claude は素の claude と同じになります。",
    "{label} 要形如 4G / 512M / infinity，收到 {value!r}": "{label} は 4G / 512M / infinity の形式が必要です。受け取った値：{value!r}",
    "不认识的配置项 {key!r}，可用：{v0}": "不明な設定キー {key!r}。使用可能：{v0}",
    "{key} 要 true/false，收到 {value!r}": "{key} は true/false が必要です。受け取った値：{value!r}",
    "{p} 读不了：{exc}": "{p} を読めません：{exc}",
    "{p}：不认识的段 [{section}]，只有 {v0}": "{p}：不明なセクション [{section}]。あるのは {v0} のみ",
    "{p}：[{section}] 应该是一个表，收到 {v0}": "{p}：[{section}] はテーブルであるべきです。受け取った型：{v0}",
    "{key} 要形如 name.slice，收到 {value!r}": "{key} は name.slice の形式が必要です。受け取った値：{value!r}",
    "{p}：[{section}] 里不认识 {name!r}，可用：{v0}": "{p}：[{section}] 内の不明なキー {name!r}。使用可能：{v0}",
    "没有正在运行的 tmux server —— 先 `tmux new -s main`，或用 --print": "実行中の tmux server がありません —— まず `tmux new -s main`、または --print",
    "找不到 pane {pane_id}": "pane {pane_id} が見つかりません",
    "不认识的会话来源: {entry_source}": "不明なセッションソース: {entry_source}",
    "这条会话的工作目录已经不存在了：{entry_cwd}\n        投递会执行 `cd` 到该目录，失败后整条命令会中止、claude 不会启动。\n        （常见原因：/tmp 下的 scratchpad 被清理、git worktree 被删、项目目录移动过）": "このセッションの作業ディレクトリはもう存在しません：{entry_cwd}\n        投入は先にそこへ `cd` します。失敗するとコマンド全体が中止され、claude は起動しません。\n        （よくある原因：/tmp のスクラッチパッドが掃除された、git worktree が削除された、プロジェクトが移動した）",
    "投递到已有 pane 需要 pane_id": "既存の pane への投入には pane_id が必要です",
    "不认识的投递目标: {target_kind}": "不明な投入先: {target_kind}",
    "绑定说明：": "バインド：",
    "（已存在 atm 配置块，会被整块替换掉，不会重复追加）": "（atm ブロックが既にあります；丸ごと置き換え、二重追加はしません）",
    "全部会话": "全セッション",
    "只看当前目录的会话": "現在のディレクトリのセッションのみ",
    "侧栏：开 / 切过去 / 收起": "サイドバー：開く / 移動 / 閉じる",
    "把当前格子收进后台窗口 bg": "現在の pane をバックグラウンド window bg に退避",
    "{path} 的备份文件名连撞 100 次，放弃。": "{path} のバックアップ名が 100 回連続で衝突しました。諦めます。",
    "--key 和 --sidebar-key 不能相同（都是 {key!r}）": "--key と --sidebar-key は同じにできません（両方 {key!r}）",
    "{begin} 有开始标记但找不到对应的 {end}。为避免误删标记之后的配置，这里不做任何改动 —— 请手动检查 ~/.tmux.conf。": "{begin} の開始マーカーはあるのに対応する {end} がありません。マーカー以降の設定を誤って消さないよう、何も変更しません —— ~/.tmux.conf を手で確認してください。",
    "{name} 只能是单个小写字母，收到 {value!r}。（配套的第二条绑定靠 .upper() 区分，非字母的 upper() 是恒等变换，两条绑定会撞成同一个键、第一条静默失效。）": "{name} は小文字 1 文字のみです。受け取った値：{value!r}。（対になる 2 つ目のバインドは .upper() で区別するため、英字以外は upper() が恒等になり、両方が同じキーに衝突して 1 つ目が黙って無効になります。）",
    "{path} 存在但读不出来（{v0}）。为避免覆盖掉你现有的配置，这里直接中止。请先确认它的编码/权限，或手动备份后删除它。": "{path} は存在しますが読めません（{v0}）。既存の設定を上書きしないよう中止します。エンコーディング / 権限を確認するか、手動でバックアップして削除してください。",
    "备份 {path} 到 {backup} 失败（{v0}: {exc}）。为避免没有退路地改你的配置，这里直接中止。": "{path} を {backup} にバックアップできませんでした（{v0}: {exc}）。戻れない変更を避けるため中止します。",
    "没找到 git": "git が見つかりません",
    "（已存在持久化块，会被整块替换掉，不会重复追加）": "（永続化ブロックが既にあります；丸ごと置き換え、二重追加はしません）",
    "将写入 {self_conf_path}（持久化块）：": "{self_conf_path} に書き込みます（永続化ブロック）：",
    "  ⚠ 没找到 git，克隆会跳过；装好 git 后重跑，或在 tmux 里按 prefix + I": "  ⚠ git がないため clone はスキップします；git を入れて再実行、または tmux 内で prefix + I",
    "{self_conf_path} 里已经有你自己的 tpm 配置，不重复写插件块；要恢复功能请自行加上 tmux-resurrect / tmux-continuum。": "{self_conf_path} には既にあなたの tpm 設定があるため、プラグインブロックは書きません。復元機能が必要なら tmux-resurrect / tmux-continuum を自分で追加してください。",
    "将 git clone 到 {self_plugins_dir}/：{names}": "{self_plugins_dir}/ に git clone します：{names}",
    "插件已在 {self_plugins_dir}/，不重复克隆": "プラグインは既に {self_plugins_dir}/ にあります。再 clone しません",
    "那是侧栏自己": "それはサイドバー自身です",
    "本窗口除了侧栏没有别的格子，没法换位 —— 先分一格出来": "この window にはサイドバー以外の pane がなく入れ替えられません —— まず一つ分割してください",
    "侧栏自己不能收进后台 —— 要收起侧栏用 `atm sidebar --toggle`": "サイドバー自身は退避できません —— 閉じるには `atm sidebar --toggle`",
    "不能换进侧栏的位置": "サイドバーの位置には入れ替えられません",
    "找不到 pane {target_id}（可能刚被关掉）": "pane {target_id} が見つかりません（閉じられた直後かもしれません）",
    "pane {pane_id} 已经在 {PARK_WINDOW} 里了": "pane {pane_id} は既に {PARK_WINDOW} にあります",
    "pane {pane_id} 已经独占一个窗口，本来就在后台，不用收": "pane {pane_id} は既に window を独占しており、もともとバックグラウンドです。退避不要",
    "找不到当前 pane {current_pane}": "現在の pane {current_pane} が見つかりません",
    "找不到目标格子 {slot_id}": "対象の枠 {slot_id} が見つかりません",
    "atm sidebar 必须在 tmux pane 里跑（用 `atm sidebar --toggle` 打开）": "atm sidebar は tmux pane 内で実行する必要があります（`atm sidebar --toggle` で開く）",
    "tmux 没装或不在 PATH 里": "tmux が未インストールか PATH にありません",
    "tmux {v0} 失败: {detail}": "tmux {v0} が失敗: {detail}",
    "tmux {v0} 超时": "tmux {v0} がタイムアウト",
    "无法执行 tmux: {exc}": "tmux を実行できません: {exc}",
    "⚠目录已删 ": "⚠dir消失 ",
    "要不要套闸门（true/false）": "ゲートを掛けるか（true/false）",
    "软上限：超了节流 + 回收，不杀（如 4G）": "ソフト上限：超えたらスロットリング + 回収、殺さない（例 4G）",
    "硬上限：回收压不住才杀，杀的是整个会话及其子进程（如 8G）": "ハード上限：回収で抑えられないときに kill。セッション全体と子プロセスが対象（例 8G）",
    "swap 上限（WSL 上 swap 在宿主 SSD，建议小）": "swap 上限（WSL では swap がホスト SSD 上にあるため小さめに）",
    "所有会话共同归入的 systemd slice，兜总量": "全セッションが所属する systemd slice。合計を抑える",
    "systemd-run 是否走 --user（一般 true）": "systemd-run が --user を使うか（通常 true）",
    "存在": "あり",
    "不存在": "なし",
    "atm 写的": "atm が作成",
    "用户自己的": "ユーザー自身の",
    "你自己的": "あなた自身の",
    " ←当前": " ←現在",
    "把「{v0}」{suffix} 投到哪里？": "「{v0}」{suffix} をどこに投入しますか？",
    "继续吗？": "続けますか？",
    "有": "あり",
    "无": "なし",
    "（跑 atm --version 看版本）": "（atm --version でバージョン確認）",
    "已装": "導入済み",
    "未装（atm install 会一起装）": "未導入（atm install が一緒に入れます）",
    "在跑": "稼働中",
    "没起来": "停止中",
    "是": "はい",
    "否": "いいえ",
    "# atm 的用户配置。改完立即生效，不用重启。": "# atm のユーザー設定。変更は即時反映、再起動不要。",
    "# `atm config` 查看，`atm config <key> <value>` 修改。": "# `atm config` で表示、`atm config <key> <value>` で変更。",
    "配置文件：{v0}{v1}": "設定ファイル：{v0}{v1}",
    "（不存在，全部默认）": "（存在せず、すべてデフォルト）",
    "优先级：命令行参数 > 环境变量（{ENV_PREFIX}MEMORY_HIGH 这类）> 文件 > 默认": "優先順位：コマンドライン引数 > 環境変数（{ENV_PREFIX}MEMORY_HIGH など）> ファイル > デフォルト",
    "提示：这条会话的转录有 {mb:.0f}MB，{v0} 可能会先问你要不要「从摘要恢复」。": "注意：このセッションの記録は {mb:.0f}MB あり、{v0} が先に「要約から復元するか」を尋ねるかもしれません。",
    "新开": "新規に開いて",
    "投递到": "投入先",
    "pane {pane_id} ({pane_label}) 里跑的是 `{pane_current_command}`，它的语法和我们生成的 POSIX 命令行（`cd '…' && …`，shlex 单引号转义）不兼容 —— 投进去不会报错，会**执行成别的东西**。用 --force 强制投递。": "pane {pane_id} ({pane_label}) では `{pane_current_command}` が動いており、生成する POSIX コマンドライン（`cd '…' && …`、shlex のシングルクォート）と構文が合いません —— エラーにならず**別のものとして実行**されます。--force で強制投入。",
    "pane {pane_id} ({pane_label}) 里正在跑 `{pane_current_command}`，不是空闲 shell —— 投进去会被当成那个程序的输入。用 --force 强制投递。": "pane {pane_id} ({pane_label}) では `{pane_current_command}` が実行中で、idle な shell ではありません —— 投入するとそのプログラムへの入力になります。--force で強制投入。",
    "将写入总量闸门 {st_path}：MemoryHigh={high} / MemoryMax={max_}（物理内存 {v0}G 的 50% / 65%），然后 systemctl --user daemon-reload。": "合計ゲート {st_path} を書きます：MemoryHigh={high} / MemoryMax={max_}（物理メモリ {v0}G の 50% / 65%）、その後 systemctl --user daemon-reload。",
    "# 软限：超了强回收 + 节流，不杀。": "# ソフト上限：超えたら回収 + スロットリング、殺さない。",
    "# 硬限：到这里才杀。宁可掉一个会话，也不要整机冻死。": "# ハード上限：ここで kill。マシン全体を凍らせるよりセッション一つを失う方がよい。",
    "总量闸门 {st_path} 已存在（MemoryHigh={st_high} / MemoryMax={st_max}，{who}），不动。": "合計ゲート {st_path} は既に存在します（MemoryHigh={st_high} / MemoryMax={st_max}、{who}）。触りません。",
    "将写入 {self_conf_path}：": "{self_conf_path} に書き込みます：",
    "用你的包管理器装 tmux（>= 3.2，需要 display-popup）": "パッケージマネージャで tmux を入れてください（>= 3.2、display-popup が必要）",
    "# 重启后恢复 session / window / pane / cwd（只搭骨架，不重新拉起 claude / codex ——": "# 再起動後に session / window / pane / cwd を復元（骨格のみ、claude / codex は再起動しない ——",
    "# 会话在对应格子里用 atm 按需 resume，别把它们加进 @resurrect-processes）": "# セッションは対応する pane で atm により必要時に resume；@resurrect-processes に入れないこと）",
    "git clone {name} 失败": "git clone {name} に失敗",
    "，旧格子是空闲 shell，已关掉": "、旧 pane は idle な shell だったので閉じました",
    "，旧格子进后台": "、旧 pane はバックグラウンドへ",
    "并入": "追加",
    "新建": "新規作成",
    "把 {self_target} 换进 {self_slot} 的位置{tail}": "{self_target} を {self_slot} の位置に入れ替えました{tail}",
    "把 {self_pane} 收进后台窗口 {PARK_WINDOW}（{verb}）": "{self_pane} をバックグラウンド window {PARK_WINDOW} に退避（{verb}）",
    "打开侧栏": "サイドバーを開きました",
    "切到 {self_target}（已在本窗口）": "{self_target} に移動（既にこの window 内）",
    "切到侧栏 {self_sidebar_id}": "サイドバー {self_sidebar_id} に移動",
    "收起侧栏 {self_sidebar_id}": "サイドバー {self_sidebar_id} を閉じました",
    "⏎换入 ^T选格 ^X收后台 Tab来源 ^R重建 ^C退": "⏎入替 ^T枠 ^X退避 Tab元 ^R再構築 ^C終了",
    "历史": "履歴",
    "数字/⏎ 选格  Esc 取消": "数字/⏎ 枠選択  Esc キャンセル",
    "本窗口除了侧栏没有别的格子": "この window にはサイドバー以外の pane がありません",
    "^X 只对运行中的格子有效": "^X は実行中の pane にのみ有効",
    "已恢复 → {result_pane_id}": "復元しました → {result_pane_id}",
    "换进哪格？(数字=prefix+q 编号)": "どの枠に入れますか？（数字 = prefix+q の番号）",
    "本窗口没有 {pane_index} 号格子": "この window に {pane_index} 番の pane はありません",
    "运行中 {v0}": "実行中 {v0}",
    "把「{v0}」换进…": "「{v0}」を入れ替え…",
    "已重建索引": "インデックスを再構築しました",
    "↑↓/^N^P 移动  Tab 切来源  ^G 切聚合(目录/agent/平铺)  Enter 选中  Esc 取消": "↑↓/^N^P 移動  Tab ソース  ^G グループ化(dir/agent/flat)  Enter 選択  Esc キャンセル",
    "↑↓ 移动  数字键直选  Enter 确认  Esc 取消": "↑↓ 移動  数字で直接選択  Enter 確定  Esc キャンセル",
    "  ⚠ resume 会让 CLI 读完整个文件": "  ⚠ resume で CLI がファイル全体を読みます",
    "⚠ 工作目录已不存在  ": "⚠ 作業ディレクトリが存在しません  ",
    "↑↓ 移动({first}-{last}/{total})  数字键直选  Enter 确认  Esc 取消": "↑↓ 移動({first}-{last}/{total})  数字で直接選択  Enter 確定  Esc キャンセル",
    "源码目录": "ソースディレクトリ",
    "认不出这份 atm 是怎么装的。手动升级：\n  uv tool upgrade {DIST_NAME}        # uv tool 装的\n  pipx upgrade {DIST_NAME}           # pipx 装的\n  pip install -U {DIST_NAME}         # pip 装的（注意 PyPI 上的 `atm` 是别人的包）": "この atm のインストール方法が判別できません。手動で更新：\n  uv tool upgrade {DIST_NAME}        # uv tool で入れた場合\n  pipx upgrade {DIST_NAME}           # pipx で入れた場合\n  pip install -U {DIST_NAME}         # pip で入れた場合（PyPI の `atm` は別人のパッケージ）",
    "这是 editable 安装（{where}），升级就是 `git pull`；改了依赖再 `uv sync --frozen`。": "これは editable インストール（{where}）です。更新は `git pull`；依存が変わったら `uv sync --frozen`。",
    "不在 PATH 里 —— 投递出去会失败": "PATH にありません —— 投入は失敗します",
}

CATALOG: dict[str, dict[str, str]] = {"en": EN, "ja": JA}
