== 1. toggle：打开通高侧栏 ==
  打开侧栏
  ✅ 侧栏 pane 存在且带 @atm_sidebar 标记
  ✅ 侧栏通高（=窗口高 40）且在最左
  ✅ 侧栏宽 30
  ✅ 侧栏 pane 里跑的是 atm（python）
  ✅ 侧栏画出了「运行中」段
== 2. toggle：焦点不在侧栏 → 切过去 ==
  切到侧栏 %3
  ✅ 活动 pane 变成侧栏
  ✅ 没有开出第二个侧栏
== 3. swap：把 bg 里的 pane 换进主格 ==
  把 %2 换进 %0 的位置，旧格子进后台
  ✅ BG 现在在 w0 里
  ✅ MAIN 被换去了 bg 窗口
  ✅ 焦点在换进来的 BG 上
  ✅ 侧栏没动（仍通高 30 列）
  ✅ swap 后进程没断（BG 的 sleep 还在）
== 4. swap：目标已在本窗口 → 只切焦点 ==
  切到 %1（已在本窗口）
  ✅ 焦点切到 OTHER
  ✅ 布局没变（w0 仍是 3 格）
== 5. park：把 OTHER 收进已有的 bg ==
  把 %1 收进后台窗口 bg（并入）
  ✅ OTHER 进了 bg
  ✅ w0 剩 侧栏+BG 两格
  ✅ 焦点仍留在 w0（-d 不切过去）
== 6. park 拒绝：侧栏自己 / 独占窗口的 pane ==
  ✅ 拒绝收侧栏
  ✅ 拒绝收独占窗口的 pane（否则窗口会被改名成 bg）
  ✅ solo 窗口还在
== 7. toggle：焦点在侧栏 → 收起，主区占满 ==
  收起侧栏 %3
  ✅ 侧栏 pane 没了
  ✅ BG 占满整行（宽 140）
== 8. swap 进空闲 bash：旧格子自动关掉，不留在后台 ==
  ✅ 有一个空闲 bash 格子
  把 %0 换进 %5 的位置，旧格子是空闲 shell，已关掉
  ✅ MAIN 换进了 bash 原来的位置（在 w0）
  ✅ 空闲 bash 被关掉了
  ✅ 没有多出窗口
== 9. --keep：空闲 bash 也留着 ==
  把 %2 换进 %6 的位置，旧格子进后台
  ✅ --keep 时 bash 仍存在（进了后台）

  A:w0.0 %6 w=70 h=40 cmd=bash sb=[]
  A:w0.1 %0 w=34 h=40 cmd=sleep sb=[]
  A:w0.2 %2 w=34 h=40 cmd=sleep sb=[]
  A:bg.0 %1 w=140 h=40 cmd=sleep sb=[]
  A:solo.0 %4 w=140 h=40 cmd=sleep sb=[]

通过 27，失败 0
